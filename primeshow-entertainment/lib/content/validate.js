const REQUIRED_FIELDS={movie:["title","slug","status","synopsis","language","genre"],article:["title","slug","author","publishedAt","category","image","sections"],quiz:["slug","movie","difficulty","questions"],award:["id","movie","category","year","description"],gallery:["id","title","image","caption"]};
const missing=(item,type)=>REQUIRED_FIELDS[type].filter(field=>item[field]===undefined||item[field]===null||item[field]==="");
function validateCollection(type,items){const errors=[],seen=new Set();const identity=type==="award"||type==="gallery"?"id":"slug";items.forEach((item,index)=>{const absent=missing(item,type);if(absent.length)errors.push(`${type}[${index}] is missing: ${absent.join(", ")}`);if(seen.has(item[identity]))errors.push(`${type} has duplicate ${identity}: ${item[identity]}`);seen.add(item[identity])});return errors}
export function validateRepository(repository){const quizzes=repository.getQuizzes();const errors=[...validateCollection("movie",repository.getMovies()),...validateCollection("article",repository.getArticles()),...validateCollection("quiz",quizzes),...validateCollection("award",repository.getAwards()),...validateCollection("gallery",repository.getGalleries())];quizzes.forEach(quiz=>quiz.questions.forEach((question,index)=>{if(!question.question)errors.push(`${quiz.slug} question ${index+1} has no prompt`);if(!Array.isArray(question.options)||question.options.length!==4)errors.push(`${quiz.slug} question ${index+1} must have four options`);if(!Number.isInteger(question.correct)||question.correct<0||question.correct>3)errors.push(`${quiz.slug} question ${index+1} has an invalid correct-answer index`)}));return{valid:errors.length===0,errors}}
